{
  "name": "5bx",
  "version": "1.0.0",
  "private": true,
  "description": "Guided 11-minute 5BX workout PWA. Static site — nothing to build or install.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec test/*.test.js",
    "verify": "node tools/verify-config.mjs",
    "stamp": "node tools/stamp-version.mjs",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  },
  "engines": {
    "node": ">=22"
  }
}
